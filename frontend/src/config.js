// 应用配置
export const config = {
  // 生产环境（APP/线上）- 使用 frp 域名 (HTTP)
  apiBaseUrl: 'http://yunwa.site:8000/api',
  socketUrl: 'http://yunwa.site:8000',

  // 开发环境（本地）
  // apiBaseUrl: '/api',
  // socketUrl: 'http://localhost:8000'
}

export default config
